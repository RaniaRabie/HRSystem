@model IEnumerable<HRSystem.BLL.ModelVM.Position.PositionVM>

@{
    ViewData["Title"] = "Positions";
}

@if (TempData["Success"] != null)
{
    <div class="alert alert-success border-0 rounded-3 py-2 small">
        <i class="bi bi-check-circle me-2"></i>@TempData["Success"]
    </div>
}

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger border-0 rounded-3 py-2 small">
        <i class="bi bi-exclamation-circle me-2"></i>@TempData["Error"]
    </div>
}

<div class="card">
    <div class="card-header bg-white border-0 py-3 px-4
        d-flex justify-content-between align-items-center">
        <span class="fw-semibold" style="font-size:14px;">
            All Positions
            <span class="badge bg-primary bg-opacity-10 text-primary ms-2 fw-normal">
                @Model.Count()
            </span>
        </span>
        <a asp-action="Create" class="btn btn-primary btn-sm">
            <i class="bi bi-plus-lg me-1"></i> Add Position
        </a>
    </div>

    <div class="card-body p-0">
        <table class="table table-hover mb-0">
            <thead>
                <tr>
                    <th>Position</th>
                    <th>Department</th>
                    <th>Employees</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @if (!Model.Any())
                {
                    <tr>
                        <td colspan="4" class="text-center text-muted py-4">
                            No positions yet
                        </td>
                    </tr>
                }

                @foreach (var pos in Model)
                {
                    <tr>
                        <td class="fw-medium">@pos.Name</td>
                        <td>
                            <span class="badge bg-primary bg-opacity-10
                                text-primary fw-normal">
                                @pos.DepartmentName
                            </span>
                        </td>
                        <td>@pos.EmployeesCount</td>
                        <td class="text-end pe-4">
                            <a asp-action="Edit" asp-route-id="@pos.Id"
                               class="btn btn-outline-primary btn-sm me-1">
                                <i class="bi bi-pencil"></i> Edit
                            </a>

                            <form asp-action="Delete" asp-route-id="@pos.Id"
                                  method="post" class="d-inline"
                                  onsubmit="return confirm('Delete @pos.Name?')">
                                @Html.AntiForgeryToken()
                                <button class="btn btn-outline-danger btn-sm">
                                    <i class="bi bi-trash"></i> Delete
                                </button>
                            </form>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>